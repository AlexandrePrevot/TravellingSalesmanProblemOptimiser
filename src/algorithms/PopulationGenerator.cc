#include "algorithms/PopulationGenerator.hpp"

#include <cstdlib>
#include <ctime>

Population PopulationGenerator::generateNewPopulation(const int population_size, double max_y, double max_x, double min_y, double min_x)
{
    std::srand(std::time(nullptr));

    Population population;

    

    for ()

    if (random_value >= m_mutation_rate) {
        return false;
    }

    if (m_mutation_policy == kSwap) {
        randomSwapMutation(individual);
    }
}